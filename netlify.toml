[build]
  base = "frontend"
  publish = "dist"
  command = "npm ci && npx vite build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--no-audit"
